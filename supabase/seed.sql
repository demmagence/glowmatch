-- Default skincare categories are inserted by the schema migration so that the
-- same data is available in local, staging, and production environments.
-- Keep this file present because `supabase db reset` loads it by default.

